<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".PatientDetails2"></activity>
        <activity android:name=".PatientAppointment" />
        <activity android:name=".AppointmentHistory" />
        <activity android:name=".PatientDetails" />
        <activity android:name=".DocDetails" />
        <activity android:name=".DocTimeTable" />
        <activity android:name=".BookAppointment" />
        <activity
            android:name=".PatientProfile"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".PatientHomePage"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".TimeTable" />
        <activity android:name=".DocProfile" />
        <activity android:name=".DocAppointment" />
        <activity android:name=".RegisterDocActivity" />
        <activity android:name=".MainActivity3" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>